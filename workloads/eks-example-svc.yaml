#aws elbv2 describe-target-health --target-group-arn arn:aws:elasticloadbalancing:eu-west-1:382076407153:targetgroup/k8s-eksexamp-eksexamp-8913b0ff27/52b10561294b2b21 --query 'TargetHealthDescriptions[].[Target.Id,TargetHealth.State]' --output table
apiVersion: v1
kind: Service
metadata:
  name: eks-example
  namespace: eks-example
  labels:
    app: eks-example
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
    service.beta.kubernetes.io/aws-load-balancer-type: 'external' # important for not using the "in-tree" code
    service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: 'ip'
    #service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: 'ip'
    # do not apply this manifest on the cluster
    flux.weave.works/ignore: "true"
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: eks-example
  #https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-readiness-gate
  #https://kubernetes-sigs.github.io/aws-load-balancer-controller/v2.2/deploy/pod_readiness_gate/
  #For readiness gate configuration to be injected to the pod spec, you need to apply the label elbv2.k8s.aws/pod-readiness-gate-inject: enabled to the pod namespace
  #readinessGates:
  #  - conditionType: "www.example.com/feature-1"
